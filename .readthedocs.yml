---
version: 2

formats: all

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

python:
  install:
    - requirements: constraints.txt
    - path: .
      extra_requirements:
        - docs
        # to autodoc jirashell
        - cli
