---
version: 2

formats: all

build:
  os: ubuntu-24.04
  tools:
    python: "3.11"

python:
  install:
    - path: .
      extra_requirements:
        - docs
        # to autodoc jirashell
        - cli

sphinx:
  configuration: docs/conf.py
