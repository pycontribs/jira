---
version: 2

formats: all

build:
  os: ubuntu-22.04
  tools:
    python: "3.8"
  jobs:
    pre_install:
      - export PIP_CONSTRAINT=constraints.txt

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
